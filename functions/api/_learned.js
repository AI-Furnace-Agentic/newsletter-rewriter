// Learned editorial rules — WRITTEN BY THE WEEKLY LEARNING JOB. Do not edit by
// hand except to delete a rule that's wrong (the learner treats the committed
// file as its prior state). History of every change lives in git.
//
// The base spec in _editorial.js is human ground truth and always wins on
// conflict. These rules are distilled every Tuesday from the editor's actual
// published versions vs our rewrites.

export const LEARNED_RULES = `
CONFIRMED
- Put a concrete figure (dollar amount, score, round size) in the headline instead of a qualitative stand-in like "budget pricing" or "big raise".
- Keep headlines to eight words or fewer.
- Build the headline around one active verb naming what the company did (Matches, Banks) and cut noun-pile phrases like "Posts Frontier Benchmark Scores".
- Keep the lead to a single sentence carrying this week's action plus one number.
- Close every story with one forward-looking sentence that names the unresolved question, and leave that sentence untouched from draft to publish.

TENTATIVE (seen once)
- Cut benchmark lists to the single most impressive number and name its test (91.2 MMLU), dropping the rest.
- Follow the surviving number with a comparison that shows what it matches or beats, instead of adding more stats.
- Spend lead space on interpretation rather than re-listing specs the headline already delivered.
- Compress pricing to the bare figure ($2) in the headline and leave the unit phrase ("per million tokens") out.
`;
